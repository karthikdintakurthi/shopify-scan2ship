// Navigation component for the Scan2Ship app
// The navigation is handled by the NavMenu in the main app layout

export function Navigation() {
  // This component is kept for future use if we need additional navigation features
  return null;
}
